# 波次路线图（W5–W8）· 唯一波次事实源

> 依据：specs/constitution.md v2.0（北极星愿景 V1–V6）+ specs/adr/0003-vision-v2-and-wave-replan.md
> 上游历史：W0 工程地基 → W1 内容底座 → W2 生产底座 → W3 学生侧业务闭环 → W4 闭环硬化/发布前就绪（均已完成，见 tasks/board.md）
> 每波的可派工单元见 `tasks/w<N>/ISSUES.md`（issue 台账：工作内容 / 验证方法 / 依赖 / 并行批次），W5/W6 已物化为任务卡。

## 一、为什么重排：先可信，再能力，后规模

W4 出口叫"发布前就绪"，但四份独立代码审查一致证明：**发布前就绪 ≠ 可发布**。当前系统存在三类阻塞：

1. **门与账的强制是假的**——内容表 `gate_certificate_id` 无外键、内容版本账缺 append-only 触发器 → 伪造证书即可发布，脏内容可进 serving 区；
2. **在线链路是裸的**——全 API 零认证、会话提交无幂等无行锁、事件写入自行 commit → 任何人可冒充学生写入不可回滚的作答账；
3. **合规是孤岛**——家长授权模块无调用方、PII 剥离 fail-open 且带 bypass 开关 → 未成年人数据与 LLM 调用同时踩线。

在这三类未闭合前扩张功能，写进三本账的每一条数据都可能是脏的——而三本账**只增不改**，脏数据不可回滚。这直接损伤 V2「数据飞轮是唯一不可复制资产」。故 **W5 是硬前置，不与任何新功能波并行**。

## 二、波次总览

| 波次 | 名称 | 目标（一句话） | 前置 | 出口证据 | 对应愿景/条款 |
|---|---|---|---|---|---|
| **W5** | 信任硬化波 | 把宪法从文档承诺变成物理强制：门/账/认证/合规/事务/测试可信度全部有实证 | W4 | 强制实证矩阵首次全绿 + 绕过门/冒充学生/伪造证书三类攻击全部被拒 | A8 D9 D10 D11 P9 X11–X13 · V2 |
| **W6** | 引擎解锁与飞轮加速波 | 真·无限出题（学科函数库注入）+ D5/D6 闭环 + 曝光与冷启动 + 内容规模化 | W5 | 同一母题产出 ≥N 种结构不同的合格实例；三场景参数分离可实证；万题级题池组卷压测达标 | A7 D5 D6 · V2 V3 |
| **W7** | 首个真实用户波 | 学生端在线闭环 + 账号/授权/权益/交付 + 运营可观测，跑出第一个真实付费闭环 | W6（部分可与 W6 并行，见 §四） | 真实设备上完成练习→弱项报告→购买→交付的端到端录像与埋点漏斗 | A9 A10 · V1 V6 |
| **W8** | 规模与增长波 | 内容规模化、B 端托管机构、自适应练习、单位经济性达标 | W7 | 覆盖度/成本/留存三张指标表达标 + 机构版跑通 | A10 · V4 V5 |

## 三、波次间依赖

```
W5 信任硬化（硬前置，独占）
  ├─→ W6 引擎解锁与飞轮加速
  │     ├─→ W7-S1..S3（学生端/账号/交付）  ← 也依赖 W5-S2 认证
  │     └─→ W8-S2 自适应练习（需 W6 参数闭环）
  └─→ W7-S6 部署与运维（仅依赖 W5，可提前）
W6 ──→ W8-S1 内容规模化 / W8-S3 机构版
W7 ──→ W8-S4 单位经济性（需真实成本与转化数据）
```

## 四、跨波并行许可（人类派工用）

- **绝对不可并行**：W5-S1（门与账物理强制）与任何写内容/写事件的任务——迁移会改表结构与触发器。
- **可提前启动**（不必等 W5 全绿，但必须等 W5-S2 认证契约冻结）：W7-S1 学生端 UI 原型、W7-S6 部署与运维骨架。
- **可与 W6 并行**：W7-S5 埋点与漏斗（只加事件，不改引擎）。
- **必须串行**：W6-S1（函数库注入）→ W6-S4（内容规模化），前者不完成，后者产出的题只是加减乘除扰动。

## 五、里程碑（以证据定义，不以时间定义）

| 里程碑 | 定义（必须可复现） |
|---|---|
| M1 可信底座 | W5 出口：实证矩阵全绿；三类攻击测试（伪证书发布 / 匿名冒充学生 / 绕过写入服务直写）全部被 DB 或认证层拒绝 |
| M2 引擎解锁 | W6 出口：抽 10 个数学母题，每个产出 ≥30 个通过校验门且结构互异的实例；组卷读 item_param 且三场景互不污染 |
| M3 真实用户 | W7 出口：≥10 名真实小学生在真实设备完成练习并拿到弱项报告；≥1 笔真实付费与交付完成 |
| M4 可规模化 | W8 出口：三科覆盖度热力图达标；单份内容全成本 < 目标阈值；机构版 ≥1 家真实试点 |

## 六、任务卡与 issue 的关系

- 一条 issue = 一张任务卡 = 一个可独立验收的派工单元（宪法 P8：2–6 小时/单 agent 单会话）。
- `tasks/w<N>/ISSUES.md` 是**派工台账**（含验证方法、依赖、并行批次、owner_module 互斥关系），可直接作为 GitHub Issue 正文。
- `tasks/w<N>/T-W<N>-XXX.md` 是**执行契约**（agent 实际读取的文件，含 spec 锚点、交付物、验收标准、非目标）。
- W7/W8 当前只有 ISSUES 台账；按 P5「波内冻结」原则，在波次启动时才物化为任务卡，避免过早冻结不成熟的设计。
- 仓库原有的 10 条 GitHub Issue（#20–#30）的逐条裁决见 `tasks/LEGACY-ISSUES.md`：其中 #24/#26/#28 的有效诉求已重写为 T-W6-012 / T-W6-008 / T-W6-009，其余关闭。

## 七、各波交付物索引

| 波次 | BRIEF | ISSUES 台账 | 任务卡 |
|---|---|---|---|
| W5 | `tasks/w5/BRIEF.md` | `tasks/w5/ISSUES.md`（32 条） | `tasks/w5/T-W5-001..029` + `T01..T03` |
| W6 | `tasks/w6/BRIEF.md` | `tasks/w6/ISSUES.md`（20 条） | `tasks/w6/T-W6-001..018` + `T01..T02` |
| W7 | `tasks/w7/BRIEF.md` | `tasks/w7/ISSUES.md`（18 条） | 待 W6 尾声物化 |
| W8 | `tasks/w8/BRIEF.md` | `tasks/w8/ISSUES.md`（14 条，候选池） | 待 W7 尾声物化 |
