# 任务卡
```yaml
id: T-W5-006
wave: W5
title: 全端点接入认证与学生主体绑定
spec: [specs/constitution.md#第二部分, specs/contracts/api/openapi-v1.yaml, tasks/w5/BRIEF.md#S2]
context_paths: [src/api/routers/, src/api/auth/, tests/contract/, tests/unit/]
deliverables: [src/api/routers/, tests/contract/test_api_authz.py]
acceptance: make accept TASK=T-W5-006
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/api/routers
depends_on: [T-W5-005]
non_goals: [新增业务端点, 修改业务逻辑, 分页与响应结构重构]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
把认证框架接到每个端点上：除健康检查外全部要求主体（X13）；`/sessions`、`/sessions/{id}/responses`、`/reports/weakness/{alias}`、`/review/*` 等必须校验主体与目标 alias 一致；教研/运维端点要求对应角色。

## 验收标准（逐条可执行）
1. 静态实证测试：遍历 FastAPI 路由表，断言除白名单（健康/指标/静态）外每个端点的依赖链都包含 `require_auth`；新增端点漏加即红。
2. 学生主体访问他人 alias 的会话、报告、复习队列一律 403；越权尝试可被审计。
3. 会话相关端点从令牌主体取 `student_alias_id`，**不再信任请求体传入的 alias**。
4. 契约测试更新：未认证请求返回 401 且响应体符合错误契约；同步在 T-W5-028 中登记契约变更。
5. 既有 API 与模拟器测试按新认证方式适配后全绿（模拟器需携带令牌，不得为通过测试而放宽认证）。
