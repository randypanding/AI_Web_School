# 任务卡
```yaml
id: T-W5-023
wave: W5
title: CI 守卫盲区修复（冻结契约 / 无排名扫描）
spec: [specs/constitution.md#第三部分, specs/contracts/FROZEN.txt, tasks/w5/BRIEF.md#S6]
context_paths: [scripts/ci/, tests/contract/, .github/workflows/contract.yml]
deliverables: [scripts/ci/check_openapi_diff.py, scripts/ci/check_no_ranking.py, tests/contract/test_ci_guards.py]
acceptance: make accept TASK=T-W5-023
accept_script:
model_floor: T1
token_budget: 400k
owner_module: scripts/ci
depends_on: []
non_goals: [新增 CI 平台, 引入静态分析框架, 全仓类型检查]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
两个守卫有洞：①OpenAPI diff 只守单个文件、且"人类批准标记"可由 agent 自行添加；②无排名静态扫描漏掉 router prefix 拼接、`sorted()`、`sort_values` 等跨用户排序路径。守卫有洞等于 P3「机器信号唯一事实」失真。

## 验收标准（逐条可执行）
1. 冻结契约守卫遍历 `FROZEN.txt` 全部条目；批准标记必须依赖仓库外部信号（如带签名的 ADR 文件 + 人类提交者），agent 单方面添加无效。
2. 无排名扫描补齐盲区：路由 prefix 拼接后的完整路径、按分数排序的表达式（`sorted`/`ORDER BY score`/`sort_values`）、跨 alias 聚合查询。
3. 自测：构造违规样例（新增排名端点、修改冻结契约）→ 守卫必须红；合法样例不误报。
4. 守卫脚本自身有单元/契约测试，纳入 `tests/contract`。
5. 不放宽任何既有检查。
