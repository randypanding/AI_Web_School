# 任务卡
```yaml
id: T-W5-028
wave: W5
title: API v1.1 契约变更申请（认证引入）
spec: [specs/constitution.md#第三部分, specs/contracts/api/openapi-v1.yaml, tasks/w5/BRIEF.md#S7]
context_paths: [specs/contracts/, specs/adr/, src/api/]
deliverables: [specs/adr/0004-api-v1.1-auth.md, specs/contracts/api/openapi-v1.1.yaml, specs/contracts/FROZEN.txt]
acceptance: make accept TASK=T-W5-028
accept_script:
model_floor: T0
token_budget: 400k
owner_module: specs/adr
depends_on: [T-W5-006]
non_goals: [新增业务端点, GraphQL, SDK 生成]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D9 引入认证必然改变 API 契约（安全方案、401/403 响应、会话端点不再接受请求体 alias），而 `openapi-v1.yaml` 已冻结。P5 要求走「契约变更申请 → 影响面分析 → 人类批准」，不得就地改冻结文件。

## 验收标准（逐条可执行）
1. ADR-0004 写明变更清单、破坏性影响（模拟器、工作台、未来小程序）、兼容期策略与回滚方案。
2. 产出 `openapi-v1.1.yaml`（新增文件，不修改 v1），`FROZEN.txt` 追加登记（只增不改）。
3. 契约测试同时校验 v1.1 与实际路由一致；`/next` 等端点补齐 A4 追溯字段（placement_token / source_ref）的显式 schema，取消 `additionalProperties: true`。
4. contract-watch 对 v1 仍然生效（v1 文件零 diff）。
5. 人类签署位留白，未签署时 ADR 状态为"待批准"。
