# 任务卡
```yaml
id: T-W5-001
wave: W5
title: 内容版本账 append-only 物理强制补齐
spec: [specs/constitution.md#第二部分, specs/adr/0003-vision-v2-and-wave-replan.md#二, tasks/w5/BRIEF.md#S1]
context_paths: [alembic/versions/, src/core/models/_base.py, tests/unit/]
deliverables: [alembic/versions/0023_content_ledger_append_only.py, tests/unit/test_content_append_only.py]
acceptance: make accept TASK=T-W5-001
accept_script:
model_floor: T0
token_budget: 800k
owner_module: alembic/versions
depends_on: []
non_goals: [新增业务表, 修改既有触发器函数语义, 数据回填]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D1「内容版本账只增不改」当前只有 `item_version` 有触发器，`item_template_version` / `material_version` / `corpus_version` 完全没有，UPDATE/DELETE 直接成功。本卡复用既有 `raise_append_only_error()` 给内容版本账全表补 `BEFORE UPDATE OR DELETE FOR EACH STATEMENT` 触发器，并同步修正 `src/core/models/_base.py` 中"DB 触发器兜底"的失实注释。

## 验收标准（逐条可执行）
1. 迁移 0023 给 `item_template_version` / `material_version` / `corpus_version`（以及审阅确认的其余内容版本表）挂 append-only 触发器，复用现有函数不新造。
2. 若某表存在合法的受控字段更新（如 `current_version_id` 指针），必须在迁移注释中写明豁免理由并以独立 append-only 表承载，禁止整表放行。
3. 测试对每张表分别断言 `UPDATE`、`UPDATE ... WHERE FALSE`、`DELETE` 三种语句均被拒绝并抛 append-only 错误。
4. `make migrate-check` 全绿；`alembic downgrade -1` 后触发器被正确移除。
5. `_base.py` 注释与实际强制范围一致（A8：没有实证不许宣称）。
