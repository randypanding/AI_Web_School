# 任务卡
```yaml
id: T-W5-024
wave: W5
title: 黄金路径端到端补齐至 ≥10 种交互类型
spec: [specs/constitution.md#第三部分, tasks/w5/BRIEF.md#S6]
context_paths: [tests/golden-path/, tests/golden/, src/core/session/, src/core/scoring/]
deliverables: [tests/golden-path/, .agent/telemetry/README.md]
acceptance: make accept TASK=T-W5-024
accept_script:
model_floor: T0
token_budget: 800k
owner_module: tests/golden-path
depends_on: [T-W5-018, T-W5-016]
non_goals: [新增交互类型, 性能压测, UI 端到端]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
P4 要求"30 抽样题型端到端链路常绿"，遥测 README 宣称 30/30，实际 `tests/golden-path/` 只有 1 个 single_choice 场景 + 1 个 `assert True` 占位。这是 A8「宪法即测试」最典型的缺口。

## 验收标准（逐条可执行）
1. 覆盖 ≥10 种注册表内交互类型（含 numeric_blank / matching / ordering / table_blank / text_blank / 听力题 等），每种走完整链路：实例化 → 过门 → 发布 → 组卷 → 会话作答 → 评分 → 事件落账 → 弱项报告可见。
2. 每条链路断言 append-only 拒绝行为与追溯码（placement_token / source_ref）完整。
3. 删除 `assert True` 占位测试，替换为真实断言（属修正而非削弱，卡内记录）。
4. 支持 `GOLDEN_PATH_QUICK=1` 快版（PR 阶段）与全量版（nightly）。
5. 同步修正 `.agent/telemetry/README.md` 中与实际覆盖不符的表述（A8）。
