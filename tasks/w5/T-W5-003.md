# 任务卡
```yaml
id: T-W5-003
wave: W5
title: 发布服务证书验真与内容寻址 fail-loud
spec: [specs/constitution.md#第二部分, specs/contracts/db/item-model.md, tasks/w5/BRIEF.md#S1]
context_paths: [src/core/content/writer.py, src/core/content/publication.py, src/core/gate/certifier/, tests/unit/]
deliverables: [src/core/content/writer.py, tests/unit/test_publish_certificate_validation.py]
acceptance: make accept TASK=T-W5-003
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/content
depends_on: [T-W5-002]
non_goals: [修改校验门策略矩阵, 新增内容类型, 重写发布事务模型]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
应用层是 DB FK 之外的第二道门，当前 `publish_item_version` 只判断证书非空；并且 A/B 级内容在缺 `template_version_digest` / `pack_digest` / `engine_digest` 时**退化为随机 UUID**，直接破坏 D3 内容寻址与全部去重/追溯/重放能力。

## 验收标准（逐条可执行）
1. 发布前校验证书：存在、未撤销、`artifact_type` 与被发布产物类型一致、`artifact_ref` 指向当前内容版本；任一不满足抛领域异常并附可诊断信息。
2. 内容寻址：公式一必填参数缺失时 **fail-loud 抛异常**，禁止任何 UUID 退化路径；`grep -rn "uuid" src/core/content/` 无用于生成内容版本 id 的路径。
3. 测试：同一内容两次发布得到同一 id；缺参发布抛异常；伪造/错配证书发布被拒。
4. 事务边界遵守 D11：本服务不自行 commit（若既有实现有 commit，一并上移）。
5. 既有 W1/W2 发布相关测试全绿。
