# 任务卡
```yaml
id: T-W5-007
wave: W5
title: 服务端凭证治理与敏感字段屏蔽
spec: [specs/constitution.md#第二部分, tasks/w5/BRIEF.md#S2]
context_paths: [src/workbench/, src/api/routers/gate.py, src/core/gate/certifier/, tests/unit/]
deliverables: [src/workbench/, tests/unit/test_credential_leakage.py]
acceptance: make accept TASK=T-W5-007
accept_script:
model_floor: T0
token_budget: 400k
owner_module: src/workbench
depends_on: [T-W5-006]
non_goals: [工作台 UI 改版, 新增工作台功能, 教研权限细分到字段级]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
当前工作台把静态 token 原文写入 `gate_certificate.issued_by` / `publication.published_by`，而这些字段经**无认证**的公开端点回传——等于把签发凭证公开。D9 明确：服务端凭证永不经 API 回传。

## 验收标准（逐条可执行）
1. 签发/发布记录改存稳定的 `operator_id`（不可反推凭证），历史数据迁移策略在卡内说明（三本账只增不改 → 只做前向修正，不改历史行）。
2. 公开端点响应模型不再暴露 `issued_by` / `published_by` 原值；教研端点可见但需 staff 角色。
3. 工作台会话 cookie：`secure=True`、`httponly=True`、`samesite` 明确；生产环境未配置凭证时启动失败。
4. 测试：扫描所有 API 响应模型字段名与示例响应，断言不含凭证类字段；用泄露的旧 token 无法通过新认证。
5. 与 T-W5-005 的主体模型一致，不新造第二套认证。
