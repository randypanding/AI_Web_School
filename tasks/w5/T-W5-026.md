# 任务卡
```yaml
id: T-W5-026
wave: W5
title: 打包与部署正确性
spec: [specs/constitution.md#第四部分, tasks/w5/BRIEF.md#S6]
context_paths: [pyproject.toml, requirements.txt, requirements-dev.txt, src/packs/]
deliverables: [pyproject.toml, docs/packaging-notes.md]
acceptance: make accept TASK=T-W5-026
accept_script:
model_floor: T1
token_budget: 400k
owner_module: pyproject.toml
depends_on: []
non_goals: [容器镜像优化, 发布到 PyPI, 依赖大版本升级]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
`pyproject.toml` 手写 packages 列表已与实际目录漂移（三份审查一致指出），且 `subject-math` 等含连字符的目录不是合法 Python 包名——本地测试靠 `importlib` 模式侥幸通过，一旦真正打包/部署即失败。

## 验收标准（逐条可执行）
1. 改用自动发现（`packages.find`）或补齐完整列表，并说明含连字符目录的加载机制（现状如何工作、部署时如何工作）。
2. `pip install .` 后在干净环境中可 `import` 核心域并运行一次最小链路（不含学科包私有路径硬编码）。
3. 依赖锁定文件与 `requirements*.txt` 头注释对齐；不新增运行时依赖。
4. `docs/packaging-notes.md` 记录学科包加载策略与已知限制。
5. 既有测试全绿；CI 不受影响。
