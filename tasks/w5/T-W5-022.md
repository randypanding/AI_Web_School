# 任务卡
```yaml
id: T-W5-022
wave: W5
title: 迁移可逆全量验证与 PR 阶段拦截
spec: [specs/constitution.md#第四部分, tasks/w5/BRIEF.md#S6]
context_paths: [Makefile, .github/workflows/pr-check.yml, .github/workflows/nightly.yml, alembic/versions/]
deliverables: [Makefile, .github/workflows/pr-check.yml, .github/workflows/nightly.yml]
acceptance: make accept TASK=T-W5-022
accept_script:
model_floor: T1
token_budget: 400k
owner_module: .github/workflows
depends_on: [T-W5-004]
non_goals: [重写历史迁移, 迁移合并压缩, 多数据库支持]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
`make migrate-check` 只 `downgrade -1`，即只验证最新一个迁移可逆；0001–0022 共 21 个中间迁移的 downgrade 路径**从未被任何 CI 触发**。X7「一切 DDL 走迁移」的可逆承诺没有实证（A8）。

## 验收标准（逐条可执行）
1. nightly 增加全量可逆验证：`upgrade head` → `downgrade base` → `upgrade head` 全绿。
2. PR 阶段保留快版（`downgrade -1`）但必须真正执行（当前 pr-check 只跑 `upgrade head`）。
3. 任一历史迁移 downgrade 失败时 CI 输出可定位到具体 revision。
4. 本卡不修改历史迁移；若发现某迁移不可逆，登记为独立任务卡而非就地改写（P5/X7）。
5. 修改后的工作流 YAML 语法校验通过，且不降低既有检查项。
