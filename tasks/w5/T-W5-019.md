# 任务卡
```yaml
id: T-W5-019
wave: W5
title: 估计器指针切换并发安全
spec: [specs/constitution.md#第二部分, tasks/w5/BRIEF.md#S4]
context_paths: [src/core/data/active_model_pointer.py, alembic/versions/0016_estimator_run.py, tests/unit/]
deliverables: [src/core/data/active_model_pointer.py, tests/unit/test_active_pointer_concurrency.py]
acceptance: make accept TASK=T-W5-019
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/data
depends_on: []
non_goals: [新增估计器, 参数融合策略调整, 全量重放性能优化]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
`set_active` 先退役旧行再插新行、无锁无唯一约束：并发切换可能出现两条活跃指针或唯一索引冲突异常，直接影响"历史报告引用当时版本"（D6）的确定性。

## 验收标准（逐条可执行）
1. 切换操作在单事务内完成并加锁（行锁或 advisory lock）；同一 scope 活跃指针唯一由 DB 部分唯一索引保证。
2. 并发测试：N 个并发切换 → 最终恰好一条活跃指针，其余请求得到明确失败或幂等成功，不出现异常泄漏。
3. 切换留痕：谁在何时把 scope 从版本 A 切到版本 B（append-only）。
4. 既有 ActiveModelPointer 与年度重放测试全绿。
5. `make migrate-check` 全绿且可逆。
