# 任务卡
```yaml
id: T-W5-025
wave: W5
title: 关键覆盖空洞补齐（并发/门/认证/API 集成）
spec: [specs/constitution.md#第三部分, tasks/w5/BRIEF.md#S6]
context_paths: [tests/unit/, tests/contract/, src/api/, src/core/gate/]
deliverables: [tests/unit/, tests/contract/]
acceptance: make accept TASK=T-W5-025
accept_script:
model_floor: T1
token_budget: 400k
owner_module: tests/unit
depends_on: [T-W5-002, T-W5-006, T-W5-018, T-W5-020]
non_goals: [覆盖率数字指标化, 重构测试框架, 引入新测试依赖]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
四份审查一致指出的覆盖空洞：并发场景、门证书 FK 拒绝、查重真实路径、API 集成（含认证）、事务回滚一致性。这些正是"测试全绿但承诺未兑现"的藏身处。

## 验收标准（逐条可执行）
1. 补齐五类测试：并发提交/并发授权/并发指针切换；伪造证书直写被 FK 拒绝；查重真实路径；带认证的 API 集成（happy + 401 + 403）；事件与会话同事务回滚。
2. 所有新测试可在 CI 的 DB 环境稳定运行，禁止 sleep 制造时序、禁止随机顺序依赖。
3. 不修改任何既有断言（X1）；如发现既有测试与实现互证，登记到卡内说明并在对应功能卡中修正。
4. `pytest --collect-only` 数量较 W4 基线净增，`scripts/wave-exit/check_no_regression.py` 基线同步更新。
5. `make accept TASK=T-W5-025` 全绿。
