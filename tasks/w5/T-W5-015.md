# 任务卡
```yaml
id: T-W5-015
wave: W5
title: TTS 链路 PII 剥离与台账对齐
spec: [specs/constitution.md#第二部分, tasks/w5/BRIEF.md#S3]
context_paths: [src/core/ai/tts/router.py, src/core/audio/producer.py, tests/unit/]
deliverables: [src/core/audio/producer.py, tests/unit/test_tts_pii.py]
acceptance: make accept TASK=T-W5-015
accept_script:
model_floor: T1
token_budget: 400k
owner_module: src/core/audio
depends_on: [T-W5-014]
non_goals: [更换 TTS 供应商, 音色扩展, ASR 口语评分]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D7/D10 对 TTS 与 LLM 一视同仁，但 TTS 合成入口不做 PII 剥离，学生姓名可随题面文本直送云供应商；缓存键只取 32 位 hex 且无 TTL/LRU，存在碰撞与无限增长风险。

## 验收标准（逐条可执行）
1. 合成入口统一经 AI 总线的剥离与台账路径，剥离失败 fail-closed。
2. 缓存键使用完整内容寻址摘要（含音色/语速/学段参数），并有容量上限与淘汰策略。
3. 测试：含姓名/手机号文本 → 供应商收到的文本已脱敏；剥离失败 → 不发起合成；相同参数命中缓存、不同参数不串音。
4. 音频门（发音正确/语速适龄）流程不受影响，既有听力链路测试全绿。
5. 不新增依赖。
