# 任务卡
```yaml
id: T-W5-014
wave: W5
title: AI 总线 fail-closed、台账全覆盖与出站加固
spec: [specs/constitution.md#第二部分, tasks/w5/BRIEF.md#S3]
context_paths: [src/core/ai/bus/router.py, src/core/ai/ledger/, src/core/ai/adapter/, tests/unit/]
deliverables: [src/core/ai/bus/router.py, src/core/ai/ledger/ledger.py, tests/unit/test_ai_bus_failclosed.py]
acceptance: make accept TASK=T-W5-014
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/ai
depends_on: []
non_goals: [更换供应商, 新增模型档位, prompt 效果调优, 异步化改造]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D10 要求"生成式调用必须经总线、必须落台账、PII 剥离失败必须 fail-closed"。现状：剥离器异常时 fail-open 放行原文，且存在 `bypass_pii_filter` 开关；`record_call` 不在 `ai_call` 内统一调用，语篇生成/量规评分/影子模式存在台账盲区；LiteLLM 默认明文 HTTP 传 master key。

## 验收标准（逐条可执行）
1. 删除 `bypass_pii_filter` 及一切等价开关（X12）；剥离器异常 → 抛出并拒绝调用，台账记录失败原因（不含原文 PII）。
2. `ai_call` 内部统一落台账：任务类型、模型标识、模型版本、prompt 版本、token、成本、关联产物 id；全仓 grep 证明没有绕过总线的直连 LLM 调用。
3. 出站强制 HTTPS（含 base_url 校验），凭证不进日志、不进异常消息、不进台账。
4. 测试：剥离失败 → 拒绝且无出站请求；每种调用档位各产生一条台账；HTTP base_url 被拒绝。
5. 既有 AI 总线/语篇/量规测试全绿。
