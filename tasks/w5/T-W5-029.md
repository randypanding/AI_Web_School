# 任务卡
```yaml
id: T-W5-029
wave: W5
title: W5 出口脚本与不退化基线
spec: [specs/constitution.md#第三部分, tasks/w5/BRIEF.md#W5 端到端出口定义]
context_paths: [scripts/wave-exit/, .github/workflows/nightly.yml, Makefile]
deliverables: [scripts/wave-exit/w5.sh, scripts/wave-exit/check_no_regression.py, Makefile]
acceptance: make accept TASK=T-W5-029
accept_script: scripts/wave-exit/w5.sh
model_floor: T0
token_budget: 800k
owner_module: scripts/wave-exit
depends_on: [T-W5-024, T-W5-025, T-W5-027, T-W5-028]
non_goals: [新增功能演示, 性能基准, 发布上线操作]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
把 W5 的 11 条端到端出口（BRIEF §W5 端到端出口定义）固化为一键脚本，并把 W0–W4 不退化检查全部纳入；测试收集数基线从 W4 更新到 W5。

## 验收标准（逐条可执行）
1. `w5.sh` 逐条执行 E2E-1..E2E-11，任一失败即非零退出并打印可定位信息。
2. 三类攻击测试（伪证书发布 / 匿名与越权访问 / 绕过写入服务直写）在脚本中显式实证。
3. 包含 W0–W4 出口脚本调用，任一退化即红。
4. `Makefile` 增加 `demo-w5` 目标；nightly 纳入 w5.sh。
5. 脚本在干净数据库上从零可跑通（不依赖开发者本地残留状态）。
