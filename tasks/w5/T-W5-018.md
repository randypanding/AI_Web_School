# 任务卡
```yaml
id: T-W5-018
wave: W5
title: 作答提交幂等与并发安全
spec: [specs/constitution.md#第二部分, tasks/w5/BRIEF.md#S4]
context_paths: [src/core/session/service.py, src/core/events/writer.py, tests/unit/]
deliverables: [src/core/session/service.py, tests/unit/test_submit_idempotency.py]
acceptance: make accept TASK=T-W5-018
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/session
depends_on: [T-W5-010, T-W5-017]
non_goals: [会话状态机重构, 离线作答同步, 断线续答产品化]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
`submit_answer` 读-改-写 `current_index` 无行锁无幂等：网络重试或双击会重复写事件、跳题、错题标记 last-write-wins。A9 要求在线链路正确性与内容资产同级。

## 验收标准（逐条可执行）
1. 提交路径对会话行加锁（`SELECT ... FOR UPDATE`）后再校验题序与推进。
2. 引入幂等键（客户端提交 id 或 `session_id + item_index + attempt`），重复提交返回首次结果且不写新事件。
3. 并发测试：10 并发提交同一题 → 恰好 1 条 `response_event`、`current_index` 推进恰好 1、返回结果一致。
4. 与 T-W5-017 的事务边界一致：整个提交是单事务。
5. 既有会话/闯关/时长保护测试全绿。
