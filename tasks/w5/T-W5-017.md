# 任务卡
```yaml
id: T-W5-017
wave: W5
title: 事件写入事务边界归位
spec: [specs/constitution.md#第二部分, specs/contracts/events/response_event.md, tasks/w5/BRIEF.md#S4]
context_paths: [src/core/events/writer.py, src/core/session/service.py, src/api/deps.py, tests/unit/]
deliverables: [src/core/events/writer.py, tests/unit/test_event_transaction_boundary.py]
acceptance: make accept TASK=T-W5-017
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/events
depends_on: []
non_goals: [引入事件总线/消息队列, 修改事件 schema, 分区策略调整]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
`record_event()` 内部 commit：作答事件先落库，随后会话状态更新失败时事件已不可回滚（append-only），造成"作答账与运行态分裂"，直接污染 V2 飞轮输入。D11 要求领域服务不自行 commit。

## 验收标准（逐条可执行）
1. 事件写入服务移除内部 commit/flush-commit 语义，事务由最外层（API 依赖或作业）统一提交。
2. 全仓静态检查：`src/core/**` 中不存在领域服务自行 `commit()` 的调用（审计类独立事务除外，需白名单登记）。
3. 测试：评分失败/会话更新失败 → 事件与会话状态同时回滚，`response_event` 计数不变。
4. 既有事件 append-only 拒绝测试与 W1 出口测试全绿。
5. 变更不改事件 schema，契约测试不受影响。
