# 任务卡
```yaml
id: T-W6-012
wave: W6
title: 组卷查询索引基线与万题级压测
spec: [tasks/w6/BRIEF.md#S5, tasks/LEGACY-ISSUES.md#24]
context_paths: [alembic/versions/, src/core/assembly/, tests/performance/]
deliverables: [alembic/versions/, tests/performance/test_assembly_scale.py]
acceptance: make accept TASK=T-W6-012
accept_script:
model_floor: T0
token_budget: 800k
owner_module: alembic/versions
depends_on: [T-W6-005]
non_goals: [引入搜索引擎, 读写分离, 分库分表]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
旧 Issue #24 提出的 JSONB GIN 方案与当前"扁平 item_kp 关联表"路线不符。本卡按现有 schema 建立真实的索引基线：先用 EXPLAIN 找出组卷/候选/曝光过滤的热点查询，再针对性建索引，并以万题级题池压测验证。

## 验收标准（逐条可执行）
1. 热点查询清单 + 优化前后的 EXPLAIN ANALYZE 证据写入报告。
2. 新增索引以 alembic 迁移落地（含 down_revision 正确串接），迁移可 upgrade/downgrade 往返。
3. 万题级题池（脚本可复现生成）下在线组卷 p95 达标，阈值写进测试断言。
4. 索引不破坏既有唯一约束与 append-only 触发器；迁移后全量测试绿。
5. 不为过压测而放宽组卷约束条件（X11）。
