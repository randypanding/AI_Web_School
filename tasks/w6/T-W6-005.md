# 任务卡
```yaml
id: T-W6-005
wave: W6
title: 组卷候选改读 item_param（D5 三场景隔离）
spec: [specs/constitution.md#第二部分, ADR/母题与知识体系-工程架构设计方案v2.md#4.4, tasks/w6/BRIEF.md#S3]
context_paths: [src/core/assembly/candidates.py, src/core/data/, tests/unit/]
deliverables: [src/core/assembly/candidates.py, tests/unit/test_assembly_reads_item_param.py]
acceptance: make accept TASK=T-W6-005
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/assembly
depends_on: []
non_goals: [新增组卷策略, CAT 自适应, 求解器替换]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D5 要求参数按 source（先验/实测）与 purpose_scope（practice/diagnosis/measurement）分开估计与使用，但组卷候选仍从 `lineage.params.p_correct_prior` 读先验——三场景共用同一个值，D5 在最关键的消费侧没有落地。

## 验收标准（逐条可执行）
1. 候选构建按场景查询 `item_param`（scope + source），缺实测时按既定融合策略回退先验，融合口径与 W4 收缩策略一致（复用不重写）。
2. 场景隔离实证：人为污染 measurement 场景参数 → practice/diagnosis 组卷结果逐题一致（固定种子）。
3. 参数缺失、样本不足、参数过期三种情况有显式行为定义与测试。
4. 组卷结果携带"本次使用了哪个参数版本"的可追溯信息（供 D6 历史回放）。
5. 既有组卷、诊断、测量测试全绿。
