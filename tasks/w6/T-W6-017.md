# 任务卡
```yaml
id: T-W6-017
wave: W6
title: 参数标定作业升级（分场景、可回放、可审计）
spec: [specs/constitution.md#第二部分, tasks/w6/BRIEF.md#S3]
context_paths: [src/core/calibration/, src/core/data/, tests/unit/]
deliverables: [src/core/calibration/, tests/unit/test_calibration_scope.py]
acceptance: make accept TASK=T-W6-017
accept_script:
model_floor: T0
token_budget: 800k
owner_module: src/core/calibration
depends_on: [T-W6-006]
non_goals: [全量 IRT 2PL/3PL 上线, 在线实时标定, 跨学科联合标定]
escalation: 失败 2 次 → 升梯队 → Judge → 人类
```

## 目标说明（≤5 行）
D5/D6 要求参数按场景分别估计、估计器可替换、历史可回放。当前标定作业未严格区分场景样本，且缺少"用哪个估计器、哪批样本、产出哪个版本"的可回放记录。

## 验收标准（逐条可执行）
1. 样本按 purpose_scope 严格分桶；跨场景样本混用在代码层被阻断并有测试。
2. 每次标定产出带 estimator 名称+版本、样本窗口、样本量、产出参数版本的记录（只增不改）。
3. 相同输入 + 相同估计器版本 → 相同输出（可回放性测试）。
4. 参数写入不覆盖历史版本，消费侧按"最新有效版本"读取。
5. 样本不足时不产出实测参数（fail-closed），并记录原因。
